ADS Viewer Pro V55 - 0206261545

Zakres: historia lotów, etapy 3 i 4.

Zrobione:
1. Etap 3 - źródło awaryjne:
   - dodano jawne sprawdzanie źródeł w kolejności: źródło wybrane w ustawieniach, ADSB.lol, ADSB.fi, ADSB.one, airplanes.live;
   - status w panelu historii pokazuje, które źródło jest właśnie sprawdzane;
   - po sukcesie program pokazuje, z którego źródła i wariantu trace pobrał dane.

2. Etap 4 - rysowanie trasy na mapie:
   - historyczna trasa jest rysowana jako rzeczywisty ślad punktowy;
   - dodano punkty na trasie z popupami: czas, wysokość, prędkość, kurs, pozycja;
   - dodano tooltipy na punktach trasy;
   - mapa automatycznie dopasowuje widok do pobranej trasy.

Nie zmieniano:
- przycisku ADS;
- działania list zapisanych samolotów;
- funkcji eksportu;
- alertów i synchronizacji.

Ograniczenia:
- trasa pojawi się tylko wtedy, gdy publiczne źródło faktycznie udostępnia trace dla danego HEX i daty;
- punkty mogą mieć braki wysokości/prędkości, jeśli źródło ich nie zwróciło.
